

# Environment & Secrets (AIOX)
.env
.env.local
.env.*.local
*.key
*.pem

# Dependencies (AIOX)
node_modules/
node_modules

# Build & Logs (AIOX)
dist/
build/
*.log
logs/

# IDE & OS (AIOX)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt

# PCH Veículos — backup do WordPress (contém credenciais do banco) e artefatos
backup-wp-pchveiculos/
docs/pch-veiculos-site.zip

# WhisperFlow (tools/whisperflow) — runtime data lives in %LOCALAPPDATA%\WhisperFlow, not the repo
tools/whisperflow/.venv/
tools/whisperflow/__pycache__/
tools/whisperflow/**/__pycache__/
